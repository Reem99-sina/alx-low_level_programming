#include <stdio.h>

/**
 *natural -  computes and prints the sum of all the
 * multiples of 3 or 5 below 1024
 *Return:  return 0
*/
int main(void)
{
int i, z;

while (i <= 1024)
{
if ((i % 3 == 0) || (i % 5 == 0))
{
z += i;
}
i++;
}
printf("%d\n", z);
return (0);
}
