#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 *print_rev -  prints a string, in reverse
 *@s:char s
 *Return:no return
*/
void print_rev(char *s)
{
int slen = strlen(s);
int i;

for (i = slen - 1; i >= 0; i--)
{
_putchar(s[i]);
}
}
