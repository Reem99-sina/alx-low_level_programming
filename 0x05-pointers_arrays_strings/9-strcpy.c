#include "main.h"
#include <string.h>
#include <string.h>
/**
 * _strcpy - copies the string  src to dest
 *@dest:string
 *@src: string
 * Return:no return
 */
char *_strcpy(char *dest, char *src)
{
strcpy(dest, src);
return (src);
}
